# React-Query Wrapper

A Way to use `react-query` with React Class Components.

Example: 
```
class Example extends React.Component {
   render(){
      return (<div>
              <UseQuery>{}</UseQuery>
              </div>);
   }
}
```
